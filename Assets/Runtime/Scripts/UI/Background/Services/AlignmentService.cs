using UnityEngine;

namespace Tempera.Mental.UI.Background
{
    public class AlignmentService : MonoBehaviour
    {
        //[SerializeField] Camera mainCam;
        //// (0,0) is bottom-left, (1,1) is top-right
        //public Vector2 screenPosition = new Vector2(0.5f, 0.5f);

        //void LateUpdate()
        //{
        //    // Convert the screen percentage to a point in the 3D world
        //    Vector3 worldPos = mainCam.ViewportToWorldPoint(new Vector3(screenPosition.x, screenPosition.y, 10f));
        //    worldPos.z = 0; // Keep it on the 2D plane
        //    transform.position = worldPos;
        //}
    }
}
